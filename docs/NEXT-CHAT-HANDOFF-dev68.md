# Mirage Projector — next-chat handoff dev.68

Current source candidate: **0.1.0-dev.68**. Network protocol remains **18**.

Live QA before this snapshot confirmed the Create Netherite Backtank synthetic chest can become fully transparent. dev.67 then focused on Beacon optics, bud beam resume offsets, Core Booster transformation plane, residual rays and Mature half-decay lighting.

dev.68 adds two targeted fixes: loaded Core Booster extraction is allowed only with empty hand or matching installed-core item, and Mature Cluster light-field teardown is immediate/overlap-aware on removal or de-energization.

The Mature Cluster half-decay field is baseline behavior at relay tier 0; Core Boosters are not required for it. If QA shows extended lighting only when a Booster is installed, treat that as a bug.

Next QA priorities: compile dev.68 on Windows; verify extraction guard with empty/matching/different held items; verify no 0.5–1 s light linger after breaking the Mature Cluster; verify overlapping fields; continue dev.67 optics QA for residual rays, bud resume heights and Booster beam geometry.
